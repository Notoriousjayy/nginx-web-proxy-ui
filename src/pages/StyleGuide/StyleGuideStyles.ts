import { CSSProperties } from 'react';

export const styleGuideStyles: CSSProperties = {
  padding: '2rem',
  display: 'grid',
  gap: '2rem',
  gridTemplateColumns: 'repeat(auto-fill, minmax(300px, 1fr))',
};
